import 'package:flutter/material.dart';

class LoginPin extends StatelessWidget {
  const LoginPin({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Login PIN'));
  }
}
